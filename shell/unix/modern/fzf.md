



```bash
branch_to_delete=$(git branch | fzf)                     # set var from pull-down(up) menu in script
branches_to_delete=$(git branch | fzf --multi)           # set vars from pull-down(up) menu in script



```

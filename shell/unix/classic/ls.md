

# ls - sort

```bash
ls -S                     # sort by size
ls --sort=size            # sort by size
ls -S -l                  # sort by size, long listing
ll -S                     # sort by size, long listing (using my ll alias)

```

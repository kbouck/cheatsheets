
# volume

```bash
sudo osascript -e "set Volume 0"       # set absolute volume - min
sudo osascript -e "set Volume 1.5"     # set absolute volume - decimal increment
sudo osascript -e "set Volume 10"      # set absolute volume - max
```

